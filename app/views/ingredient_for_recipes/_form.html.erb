  <%= simple_form_for([@recipe, @ingredient_for_recipe]) do |f| %>
    <%= f.input :amount %>
    <%= f.association :ingredient %>
    <%= f.association :unit %>
    <%= f.submit %>
  <% end %>

<%= link_to "Need more?", new_ingredient_path, class: "btn btn-default" %>