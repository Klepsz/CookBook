  <%= simple_form_for([@recipe, @ingredient_for_recipe]) do |f| %>
    <%= f.input :amount %>
    <%= f.association :ingredient %>
    <%= f.association :unit %>
    <%= f.submit %>
  <% end %>
<br>
<%= link_to "Need more ingredients?", new_ingredient_path, class: "btn btn-default" %>