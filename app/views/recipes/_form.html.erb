<%= hidden_field_tag 'tag_list', @tags, id: 'tag_list' %>

<%= simple_form_for(@recipe, html: {class: "form-horizontal"}) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :servings, label: "Servings:" %>
  <%= f.input :vegan, label: "Vegan?" %>
  <%= f.input :directions %>
  <%= f.input :tag_list, placeholder: "       " %>
  <%= f.button :submit %>
<% end %>

